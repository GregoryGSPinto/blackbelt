# Test Execution Report

## Executado

- `pnpm install`: aprovado com warnings
- `pnpm lint`: aprovado
- `pnpm test`: aprovado
- `pnpm build`: aprovado
- `pnpm build:mobile`: reprovado
- `npx cap sync ios`: reprovado por ausência de `out/`
- `npx cap sync android`: reprovado por ausência de `out/`

## Totais

- Cenários aprovados: 4
- Cenários reprovados: 3
