'use client';

/**
 * Security Dashboard — Developer/Support view
 * Re-exports the admin SecurityDashboardPage so SUPPORT/SYS_AUDITOR
 * users can access it under the (developer) layout.
 */
export { default } from '@/app/(admin)/seguranca/page';
