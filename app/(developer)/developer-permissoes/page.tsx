'use client';

/**
 * Permissões — Developer/Support view
 * Re-exports the admin PermissoesPage so SUPPORT/SYS_AUDITOR
 * users can access it under the (developer) layout.
 */
export { default } from '@/app/(admin)/permissoes/page';
